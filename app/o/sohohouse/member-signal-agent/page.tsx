import { redirect } from 'next/navigation'

/** Org-pattern alias → canonical pitch funnel (back-cover QR). */
export default function SohoMemberSignalAgentRedirectPage() {
  redirect('/soho-house-ai-assistant')
}
