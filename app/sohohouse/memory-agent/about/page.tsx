import { redirect } from 'next/navigation'

/** Short alias → canonical Soho Member Signal Agent about page. */
export default function SohoHouseMemoryAgentAboutRedirectPage() {
  redirect('/o/sohohouse/memory-agent/about')
}
