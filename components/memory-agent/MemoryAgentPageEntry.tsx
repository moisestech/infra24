/**
 * @deprecated Use server page `dynamic(MemoryAgentShell, { ssr: false })` instead.
 * Kept for imports/tests only.
 */
export { MemoryAgentShell as MemoryAgentPageEntry } from '@/components/memory-agent/MemoryAgentShell'
