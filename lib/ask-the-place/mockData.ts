/**
 * Mock entities for future graph expansion — graph nodes are built in `configs.ts` for now.
 */
export const MOCK_PLACE_NOTE =
  'Replace with Supabase / Airtable sync: Places, People, Events, Spaces, Partners.'
