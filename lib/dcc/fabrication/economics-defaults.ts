/** Internal machine allocation rate (USD/hr). Never render on client pages. */
export const MACHINE_HOURLY_USD = 15

/** Internal operator rate (USD/hr). Never render on client pages. */
export const OPERATOR_HOURLY_USD = 35

/** Warn when founder margin per hour falls below this threshold. */
export const FOUNDER_MARGIN_WARNING_USD = 125

/** Stripe-style card fee estimate — not a live charge. */
export const PAYMENT_FEE_RATE = 0.029
export const PAYMENT_FEE_FIXED_USD = 0.3
